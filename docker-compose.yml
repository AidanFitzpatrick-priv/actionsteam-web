services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: actions
      POSTGRES_PASSWORD: actions_dev
      POSTGRES_DB: actions
    ports:
      - "5432:5432"
    volumes:
      - actions_pg_data:/var/lib/postgresql/data

volumes:
  actions_pg_data:
